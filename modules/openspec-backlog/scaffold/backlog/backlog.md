# Backlog Ledger

Read [README.md](README.md) before editing. This ledger is the only authority for development
order; OpenSpec owns status (`openspec list` = in flight, `openspec/changes/archive/` = done).
The pointer target is the status; this ledger points, it never copies.

## In flight

| Item | Depends on | Pointer / state |
|---|---|---|

## Upcoming

| Item | Depends on | Pointer / state |
|---|---|---|
