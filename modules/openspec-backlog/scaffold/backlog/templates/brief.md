# <item-name>

> **Preliminary capture — deepen with /opsx:brief before propose.**

<!-- Status blockquote (above): keep exactly one, updated in place, matching the ledger row's
     state label:
       > **Preliminary capture — deepen with /opsx:brief before propose.**   (lite)
       > **Briefed <YYYY-MM-DD>.**                                           (briefed)
       > **Deep-briefed <YYYY-MM-DD>.**                                      (deep) -->

**Depends on:** —

<!-- Mirror the ledger row exactly: bare change names = hard deps; `soft:` prefix = sequencing
     preference; `—` = none. If row and brief disagree, fix whichever is wrong — loudly. -->

## Intended outcome

<!-- The slice of the user's ideal end-state this item advances — its waypoint on the path from
     today's state (0) to that ideal (1). Records INTENT, not scope: what "fully realized" looks
     like for the outcome this item serves, and why this item is a step toward it. Depth-agnostic —
     works with or without the pillars module. Downstream (/opsx:brief, propose) reads this to
     judge whether the declared depth truly serves the end-state, and whether further items at
     other depths are warranted. -->

## Objective

<!-- What this item delivers and for whom, in plain language. Outcome, not implementation.
     If the objective needs "and" more than once, this is probably two items — split it. -->

## Shape of the approach

<!-- The current opinionated take on how to build it: the seams it touches, what it
     deliberately does NOT touch, and the owner boundaries with neighboring items. Mutable —
     refine through conversation and again at pick-up time. -->

## Requirements

<!-- Refined, testable statements — each one falsifiable, none decorative. A preliminary
     capture may hold rough bullets; /opsx:brief sharpens them before propose. -->

## Open questions & risks

<!-- Open questions stated as open questions — never silently resolved by optimism. Note what
     can answer each one (a spike, a doc, the user). -->

## Pointers

<!-- Authority-tagged links, one per line — point, don't copy:
     - **SPEC:** openspec/specs/<area>/spec.md — current truth this item must respect
     - **CODE:** <path> — the seam this item lands on
     - **DOC:** <path> — direction or reference material (scope input, never order)
     - **PRODUCT:** <path or requirement id> — the product need this item serves -->

<!-- Only when the pillars module is installed (openspec/pillars/ exists), add:

## Pillars

The names of the pillars this item leans on, resolved through
openspec/pillars/DEFINITIONS.md. -->

<!-- Deep mode only — /opsx:brief with `deep` / `best practices` / `deep research` adds:

## Architecture & Ecosystem (deep research)

Current accepted patterns for this problem, candidate libraries/approaches with tradeoffs and
citations, and one opinionated recommendation with its reasoning. -->
